# Pinned deliberately: unpinned installs were resolving differently on every
# rebuild, which made "works locally / fails on deploy" impossible to diagnose.
flask==3.1.3
flask-cors==6.0.5
python-dotenv==1.2.3
google-genai==2.20.0
gunicorn==26.2.0
pillow==12.1.0
